{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/challenge3/docker/api/package.json",
                "localRoot": "${workspaceFolder}/challenge3/docker/api"
            }
        }
    ]
}